#include "../include/linear_graph.h"

int main(void)
{
        Linear_graph_without_direction    list(9, 14);
        //  Bfs(list);
        //  prim(list);
        kruskal(list);

        return 0;
 }